; The system goes into an infinite loop because the query system will attempt to unify (outranked-by ?middle-manager ?boss) with the conclusion and the previous or query. It expands
; the outranked-by query indefinitely because the system can come up with a binding for ?middle-manager that always fails the first or clause.
