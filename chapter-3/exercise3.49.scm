; Let's say you have a concurrent tree sorting algorithm with two workers. One worker grabs onto one node while another worker
; grabs onto another node. A deadlock would occur if both workers needed to access the node that the other worker acquired.
