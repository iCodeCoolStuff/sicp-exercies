; Part 1:
; Cons adds the most recent element to the beginning of answer, so the returned list will be in reverse order.

; Part 2:
; Cons now conses answer into the car position instead of the cdr position.
